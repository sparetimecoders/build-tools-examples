steps:
  - command: |-
      build
    plugins:
      - docker#v3.3.0:
          image: buildtools/build-tools
          workdir: /app
          volumes:
            - "/var/run/docker.sock:/var/run/docker.sock"
    label: build
